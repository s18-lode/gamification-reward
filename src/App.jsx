import AppRoutes from './routes/AppRoutes'

// Main application component that renders the app routes, 
// serving as the entry point for the React application
export default function App() {
  return <AppRoutes />
}
